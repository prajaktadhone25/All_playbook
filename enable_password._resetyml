---
##This script is used to reset password and again use command of no enable secret###
- name: login CSR1kv with enable password cisco
  hosts: router
  become: yes
  become_method: enable
  vars:
    ansible_become_pass: cisco

  tasks:
    - name: change password to cisco!
      ios_config:
        commands:
          - enable secret cisco!

- name: test the login with changed password
  hosts: router
  become: yes
  become_method: enable
  vars:
    ansible_become_pass: cisco!

  tasks:
    - name: remove enable secret configuration
      ios_config:
        commands:
          - no enable secret




